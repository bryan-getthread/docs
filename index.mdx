---
title: "Thread Documentation"
description: "Everything you need to set up, deploy, and master Thread."
mode: "custom"
---

<div style={{ fontFamily: "'Montserrat', sans-serif", minHeight: "100vh", padding: "0 0 80px" }}>

  {/* ── Header ── */}
  <div style={{
    padding: "56px 10% 48px",
    borderBottom: "1px solid var(--color-border-tertiary)",
    position: "relative", overflow: "hidden",
    background: "linear-gradient(135deg, rgba(0,187,153,0.08) 0%, transparent 60%)",
  }}>
    <div style={{
      position: "absolute", inset: 0,
      background: "radial-gradient(ellipse 60% 100% at 0% 50%, rgba(0,187,153,0.1) 0%, transparent 65%), radial-gradient(ellipse 40% 80% at 100% 0%, rgba(0,187,153,0.05) 0%, transparent 60%)",
      pointerEvents: "none",
    }} />
    <div style={{ position: "relative", zIndex: 1, display: "grid", gridTemplateColumns: "1fr 1fr", gap: "56px", alignItems: "center" }}>

      {/* Left: copy */}
      <div>
        <p style={{ fontSize: "10px", fontWeight: "700", letterSpacing: "0.18em", textTransform: "uppercase", color: "#00BB99", margin: "0 0 14px" }}>Thread Documentation</p>
        <h1 style={{ fontSize: "clamp(26px, 3.5vw, 40px)", fontWeight: "800", lineHeight: "1.1", margin: "0 0 14px", color: "var(--color-text-primary)", letterSpacing: "-0.03em" }}>
          What do you need<br />help with?
        </h1>
        <p style={{ fontSize: "14px", lineHeight: "1.75", color: "var(--color-text-secondary)", margin: 0, maxWidth: "380px" }}>
          Guides, references, and walkthroughs for every role — admins, technicians, dispatchers, and service managers.
        </p>
      </div>

      {/* Right: Ask AI panel */}
      <div style={{
        background: "var(--color-background-secondary)",
        border: "1px solid var(--color-border-tertiary)",
        borderRadius: "14px", padding: "24px",
      }}>
        <div style={{ display: "flex", alignItems: "center", gap: "8px", marginBottom: "16px" }}>
          <div style={{ width: "7px", height: "7px", borderRadius: "50%", background: "#00BB99" }} />
          <span style={{ fontSize: "10px", fontWeight: "700", letterSpacing: "0.14em", textTransform: "uppercase", color: "var(--color-text-tertiary)" }}>Ask AI</span>
        </div>
        {[
          ["How do I connect my PSA?", "/get-started/admin/connect-your-psa"],
          ["How do I configure Magic Agents?", "/ai/magic-agents/configure"],
          ["How do I deploy the Teams app?", "/channels/teams/deploy"],
        ].map(([q, href]) => (
          <a key={q} href={href} style={{
            display: "flex", alignItems: "center", justifyContent: "space-between",
            width: "100%", background: "var(--color-background-tertiary)",
            border: "1px solid var(--color-border-tertiary)", borderRadius: "8px",
            padding: "10px 12px", marginBottom: "8px",
            fontSize: "12px", fontWeight: "500", color: "var(--color-text-secondary)",
            textDecoration: "none",
          }}>
            {q}
            <svg width="11" height="11" viewBox="0 0 12 12" fill="none" style={{ flexShrink: 0, marginLeft: "8px" }}>
              <path d="M2.5 6h7M6 2.5L9.5 6 6 9.5" stroke="#00BB99" strokeWidth="1.5" strokeLinecap="round" strokeLinejoin="round"/>
            </svg>
          </a>
        ))}
        <div style={{ position: "relative", marginTop: "12px" }}>
          <input
            type="text"
            placeholder="Search Thread docs..."
            onKeyDown={(e) => {
              if (e.key === "Enter" && e.target.value.trim()) {
                window.location.href = `?search=${encodeURIComponent(e.target.value.trim())}`;
              }
            }}
            onClick={() => {
              const kbd = document.querySelector('button[aria-label="Search"]') || document.querySelector('[class*="SearchButton"]');
              if (kbd) kbd.click();
            }}
            style={{
              width: "100%",
              background: "rgba(0,187,153,0.05)",
              border: "1px solid rgba(0,187,153,0.2)",
              borderRadius: "9px",
              padding: "11px 40px 11px 14px",
              fontSize: "12.5px", color: "var(--color-text-primary)",
              outline: "none", boxSizing: "border-box",
              fontFamily: "'Montserrat', sans-serif",
              cursor: "text",
            }}
          />
          <div style={{ position: "absolute", right: "12px", top: "50%", transform: "translateY(-50%)", opacity: 0.5 }}>
            <svg width="14" height="14" viewBox="0 0 16 16" fill="none">
              <path d="M8 1l1.5 4.5H14l-3.75 2.75 1.5 4.5L8 10 4.25 12.75l1.5-4.5L2 5.5h4.5L8 1z" fill="#00BB99"/>
            </svg>
          </div>
        </div>
        <p style={{ fontSize: "10px", color: "var(--color-text-tertiary)", margin: "8px 0 0", textAlign: "center" }}>Press ⌘K anywhere to search</p>
      </div>

    </div>
  </div>

  {/* ── Role cards — primary wayfinding ── */}
  <div style={{ padding: "44px 10%", borderBottom: "1px solid var(--color-border-tertiary)" }}>
    <p style={{ fontSize: "10px", fontWeight: "700", letterSpacing: "0.18em", textTransform: "uppercase", color: "var(--color-text-tertiary)", margin: "0 0 20px" }}>Start here</p>
    <div style={{ display: "grid", gridTemplateColumns: "repeat(4, 1fr)", gap: "12px" }}>
      {[
        {
          role: "Admin",
          tag: "Setup & configuration",
          desc: "Connect your PSA, configure AI, deploy channels, and roll out to customers.",
          href: "/get-started/admin/quickstart",
          accent: true,
        },
        {
          role: "Technician",
          tag: "Day-to-day work",
          desc: "Navigate the Inbox, manage threads, and use AI tools to resolve faster.",
          href: "/get-started/technician/overview",
          accent: false,
        },
        {
          role: "Dispatcher",
          tag: "Triage & routing",
          desc: "Manage the queue, assign tickets, and use AI-assisted routing.",
          href: "/get-started/dispatcher/overview",
          accent: false,
        },
        {
          role: "Service Manager",
          tag: "Oversight & reporting",
          desc: "Monitor team performance, review AI accuracy, and manage flows.",
          href: "/get-started/service-manager/overview",
          accent: false,
        },
      ].map(({ role, tag, desc, href, accent }) => (
        <a key={role} href={href} style={{
          display: "flex", flexDirection: "column",
          background: accent ? "rgba(0,187,153,0.07)" : "var(--color-background-secondary)",
          border: `1px solid ${accent ? "rgba(0,187,153,0.3)" : "var(--color-border-tertiary)"}`,
          borderTop: `2px solid ${accent ? "#00BB99" : "var(--color-border-secondary)"}`,
          borderRadius: "12px", padding: "22px 20px 18px",
          textDecoration: "none", minHeight: "180px",
          justifyContent: "space-between",
        }}>
          <div>
            <p style={{ fontSize: "13px", fontWeight: "800", color: accent ? "#00BB99" : "var(--color-text-primary)", margin: "0 0 4px", display: "block", letterSpacing: "-0.01em" }}>{role}</p>
            <p style={{ fontSize: "10px", fontWeight: "600", letterSpacing: "0.1em", textTransform: "uppercase", color: "var(--color-text-tertiary)", margin: "0 0 12px", display: "block" }}>{tag}</p>
            <p style={{ fontSize: "12px", color: "var(--color-text-secondary)", margin: 0, lineHeight: "1.7", display: "block" }}>{desc}</p>
          </div>
          <div style={{ display: "flex", alignItems: "center", gap: "5px", marginTop: "16px" }}>
            <span style={{ fontSize: "11px", fontWeight: "600", color: accent ? "#00BB99" : "var(--color-text-tertiary)" }}>Get started</span>
            <svg width="11" height="11" viewBox="0 0 12 12" fill="none">
              <path d="M2.5 6h7M6 2.5L9.5 6 6 9.5" stroke={accent ? "#00BB99" : "var(--color-text-tertiary)"} strokeWidth="1.5" strokeLinecap="round" strokeLinejoin="round"/>
            </svg>
          </div>
        </a>
      ))}
    </div>
  </div>

  {/* ── Quick links — two column ── */}
  <div style={{ padding: "44px 10%", borderBottom: "1px solid var(--color-border-tertiary)", display: "grid", gridTemplateColumns: "1fr 1fr", gap: "48px" }}>

    {/* Most visited */}
    <div>
      <p style={{ fontSize: "10px", fontWeight: "700", letterSpacing: "0.18em", textTransform: "uppercase", color: "var(--color-text-tertiary)", margin: "0 0 20px" }}>Most visited</p>
      {[
        ["Connect your PSA", "/get-started/admin/connect-your-psa"],
        ["Deploy the Teams App", "/channels/teams/deploy"],
        ["Configure Magic Agents", "/ai/magic-agents/configure"],
        ["Set up Voice AI", "/ai/voice/setup"],
        ["Getting started with Inbox", "/get-started/technician/inbox-overview"],
        ["AI Privacy & Security", "/security/ai-privacy"],
      ].map(([label, href]) => (
        <a key={href} href={href} style={{
          display: "flex", alignItems: "center", justifyContent: "space-between",
          fontSize: "13px", fontWeight: "500",
          color: "var(--color-text-primary)",
          textDecoration: "none", padding: "11px 0",
          borderBottom: "1px solid var(--color-border-tertiary)",
        }}>
          {label}
          <svg width="12" height="12" viewBox="0 0 12 12" fill="none" style={{ flexShrink: 0, marginLeft: "8px" }}>
            <path d="M2.5 6h7M6 2.5L9.5 6 6 9.5" stroke="#00BB99" strokeWidth="1.5" strokeLinecap="round" strokeLinejoin="round"/>
          </svg>
        </a>
      ))}
    </div>

    {/* Integrations */}
    <div>
      <p style={{ fontSize: "10px", fontWeight: "700", letterSpacing: "0.18em", textTransform: "uppercase", color: "var(--color-text-tertiary)", margin: "0 0 20px" }}>Integrations</p>
      {[
        ["ConnectWise", "PSA ticketing, pods, permissions.", "/integrations/connectwise/setup"],
        ["Autotask", "Setup, insights, email workflows.", "/integrations/autotask/setup"],
        ["HaloPSA", "Setup, action mapping, response options.", "/integrations/halopsa/setup"],
        ["Microsoft 365", "Auth, Teams app, companion app.", "/integrations/microsoft-365"],
        ["IT Glue", "Knowledge base integration.", "/integrations/it-glue"],
        ["TimeZest", "Scheduling integration.", "/integrations/timezest"],
      ].map(([title, desc, href]) => (
        <a key={href} href={href} style={{
          display: "flex", alignItems: "center", justifyContent: "space-between",
          gap: "12px", padding: "11px 0",
          borderBottom: "1px solid var(--color-border-tertiary)",
          textDecoration: "none",
        }}>
          <div>
            <p style={{ fontSize: "13px", fontWeight: "600", color: "var(--color-text-primary)", margin: "0 0 2px", display: "block" }}>{title}</p>
            <p style={{ fontSize: "11.5px", color: "var(--color-text-tertiary)", margin: 0, display: "block" }}>{desc}</p>
          </div>
          <svg width="12" height="12" viewBox="0 0 12 12" fill="none" style={{ flexShrink: 0 }}>
            <path d="M2.5 6h7M6 2.5L9.5 6 6 9.5" stroke="var(--color-text-tertiary)" strokeWidth="1.5" strokeLinecap="round" strokeLinejoin="round"/>
          </svg>
        </a>
      ))}
    </div>

  </div>

  {/* ── New to Thread banner ── */}
  <div style={{ padding: "44px 10% 0" }}>
    <div style={{
      display: "flex", alignItems: "center", justifyContent: "space-between",
      background: "rgba(0,187,153,0.06)",
      border: "1px solid rgba(0,187,153,0.2)",
      borderRadius: "12px", padding: "28px 32px",
    }}>
      <div>
        <p style={{ fontSize: "15px", fontWeight: "800", color: "var(--color-text-primary)", margin: "0 0 6px", display: "block", letterSpacing: "-0.02em" }}>New to Thread?</p>
        <p style={{ fontSize: "13px", color: "var(--color-text-secondary)", margin: 0, display: "block", lineHeight: "1.6" }}>Follow the admin setup guide to go from zero to live in under a day.</p>
      </div>
      <a href="/get-started/admin/quickstart" style={{ flexShrink: 0, marginLeft: "32px", background: "#00BB99", color: "#000", fontWeight: "700", fontSize: "13px", padding: "11px 22px", borderRadius: "9px", textDecoration: "none", whiteSpace: "nowrap" }}>
        Admin setup guide →
      </a>
    </div>
  </div>

</div>